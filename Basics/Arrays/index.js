// Array is used to store a list
// {} -> Object litrals
// [] -> Array literals

// JavaScript is a dynamic language so the values of the variables can change at run time

let selectedColors = ["red", "blue"];
selectedColors[2] = 3;
console.log(selectedColors);
