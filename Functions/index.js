// set of statements that performs task
//Diffrence b/w Parameter and Argument.

function greet(name, lastName) {
  console.log("Hello " + name + lastName);
}

greet("Jhon ", "Smith");
greet("Marry");

// In the above example name is a parameter where as Jhon is an arugument
// Valu assigned to a function is called argument
// name is the parameter of a greet function which is passed at the time of declaration
